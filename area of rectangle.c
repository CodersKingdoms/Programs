#include<stdio.h>
main()
{
    int l,b,area;
    printf("enter the length and breadth of rectangle");
    scanf("%d%d",&l,&b);
    area = l*b;
    printf("\n area of rectangle is %d",area);

}
